# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TodoList' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RxSharingSession
  pod "Apollo", :git => "https://github.com/apollographql/apollo-ios.git", :tag => "0.13.0"
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'RxDataSources'
end
